# Variables de entorno para integración con Momentum GPS API
# Copiar este archivo a .env en el directorio backend/ y configurar con valores reales

# ===========================================
# CONFIGURACIÓN DE MOMENTUM GPS API
# ===========================================

# URL base de la API de Momentum (verificar con Momentum)
MOMENTUM_BASE_URL=https://api.momentum.com

# Credenciales de autenticación (obtener de Momentum)
MOMENTUM_TENANT_ID=your-tenant-id-here
MOMENTUM_USERNAME=your-username-here
MOMENTUM_PASSWORD=your-password-here

# Alternativamente, si Momentum proporciona un JWT token directo:
MOMENTUM_JWT_TOKEN=your-jwt-token-here

# API Key si es requerida (verificar con Momentum)
MOMENTUM_API_KEY=your-api-key-here

# ===========================================
# CONFIGURACIÓN DE EMAIL (Nodemailer)
# ===========================================

# Configuración del servidor SMTP para envío de emails
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Email por defecto para envío de reportes
DEFAULT_FROM_EMAIL=sistema@tucompania.com

# ===========================================
# CONFIGURACIÓN DE BASE DE DATOS
# ===========================================

# Si estás usando una base de datos externa
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your-db-password
DB_NAME=graphical_system

# ===========================================
# OTRAS CONFIGURACIONES
# ===========================================

# Puerto del servidor (opcional, por defecto 5000)
PORT=5000

# Entorno de ejecución
NODE_ENV=production

# Habilitar logs de debug
DEBUG_MODE=true
